import Link from 'next/link';
import Image from 'next/image';
import { Container, Box, Typography,Grid, Button } from '@mui/material';

export default function administrarHorarioProfesor() {
  return (
      /* Aca se haria la pagina donde se edita los horarios de los profesores */
      <div>
    </div>
  );
}